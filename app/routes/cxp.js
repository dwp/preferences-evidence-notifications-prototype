const express = require('express')
const router = express.Router()

// const verNum = 'v1'

// router.get(`/${verNum}/createUCClaim/account-gather`, (req, res) => {
//   req.session.gatherTodos = req.session.gatherTodos || gatherData;
//   let pageData = {
//     lastCompleted: req.session.lastCompleted,
//     gatherTodos: req.session.gatherTodos,
//     allCompleted: req.session.allCompleted,
//   };
//   res.render(`${verNum}/createUCClaim/account-gather`, pageData);
// })

module.exports = router
